machine:
  php:
    version: 5.6.14
  services:
    - mysql

dependencies:
  override:
    - echo 0

test:
  override:
    - cd /home/ubuntu
    - composer create-project --prefer-dist laravel/laravel laravel
    - cp -rfa mconsole/circle/* laravel
    - mv mconsole test/workbench/milax
    - cd laravel
    - composer update --no-scripts
    - sudo npm install -g gulp
    - npm install
    - cd workbench/milax/mconsole
    - gulp
    - phpunit